Django>=6.1,<6.2
django-environ>=0.12,<1.0
cryptography>=44.0
Pillow>=11.0

# Parsing sûr de l'export Apple Health (#70) : un fichier déposé par
# l'utilisateur reste une entrée non fiable, à l'abri des attaques XML
# classiques (entités externes, expansion d'entités) que xml.etree ne
# filtre pas par défaut.
defusedxml>=0.7,<1.0

# SSO OpenID Connect, activé uniquement si OIDC_ENABLED=True.
mozilla-django-oidc>=5.0,<6.0

# SDK officiel pour Anthropic ; les autres fournisseurs sont appelés en REST via httpx.
anthropic>=0.116
httpx>=0.28,<1.0
gunicorn>=26.2.0
whitenoise[brotli]>=6.9
