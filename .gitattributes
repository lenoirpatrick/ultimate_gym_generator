# Les scripts exécutés dans un conteneur Linux doivent garder des fins de ligne
# LF, même clonés sous Windows — sinon le shebang casse au démarrage.
*.sh text eol=lf
docker/entrypoint.sh text eol=lf
